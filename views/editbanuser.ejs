<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Update Product Details</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
    <style>
        body {
            background-color: #f4f6f9;
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
        }

        .navbar {
            background-color: #343a40 !important;
        }

        .navbar-brand,
        .nav-link {
            color: white !important;
        }

        .navbar-brand {
            font-size: 1.6rem;
            font-weight: bold;
        }

        h2 {
            font-weight: bold;
            color: #343a40;
        }

        .form-container {
            max-width: 600px;
            margin: 50px auto;
            padding: 30px;
            background-color: #ffffff;
            border-radius: 12px;
            box-shadow: 0 2px 15px rgba(0, 0, 0, 0.1);
        }

        .form-label {
            font-weight: 600;
        }

        .form-control {
            background-color: white;
            color: black;
        }

        .btn-primary {
            background-color: #6c757d;
            border-color: #6c757d;
        }

        .btn-primary:hover {
            background-color: #5a6268;
            border-color: #545b62;
        }

        .btn {
            width: 100%;
        }

        .text-center h2 {
            margin-bottom: 20px;
        }
    </style>
</head>
<body>
    <nav class="navbar navbar-expand-sm navbar-dark">
        <div class="container-fluid">
            <a class="navbar-brand" href="/">User App</a>
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarNav">
                <ul class="navbar-nav me-auto">
                    <li class="nav-item">
                        <a class="nav-link" href="/">Home</a>
                    </li>
                </ul>
            </div>
        </div>
    </nav>

    <div class="container">
        <div class="form-container">
            <h2 class="text-center">Edit Ban User</h2>
            <form action="/editbanuser/<%= banuser.banuserid %>" method="POST">

                <div class="mb-3">
                    <label for="username" class="form-label">Username</label>
                    <input type="text" id="username" name="username" class="form-control" required value="<%= banuser.username %>">
                </div>

                <div class="mb-3">
                    <label for="email" class="form-label">Email</label>
                    <input type="text" id="email" name="email" class="form-control" required value="<%= banuser.username %>">
                </div>

                <div class="mb-3">
                    <label for="ban_reason" class="form-label">Reason For Ban</label>
                    <textarea id="ban_reason" name="ban_reason" class="form-control" rows="5" required><%= banuser.ban_reason %></textarea>
                </div>

                <div class="mb-3">
                    <label for="ban_start" class="form-label">Ban Start Date:</label>
                    <input type="date" id="ban_start" name="ban_start" class="form-control" required value="<%= new Date(banuser.ban_start).toISOString().split('T')[0] %>">
                </div>

                <div class="mb-3">
                    <label for="ban_end" class="form-label">Ban End Date:</label>
                    <input type="date" id="ban_end" name="ban_end" class="form-control" required value="<%= new Date(banuser.ban_end).toISOString().split('T')[0] %>">
                </div>

                <div class="text-center">
                    <button type="submit" class="btn btn-primary px-4">Edit Ban</button>
                </div>
            </form>
        </div>
    </div>

</body>
</html>
