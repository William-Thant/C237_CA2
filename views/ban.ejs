<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
    <title>User App</title>
    <style>
        body {
            background-color: #f4f6f9;
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
        }

        .navbar {
            background-color: #343a40 !important;
        }

        .navbar-brand,
        .nav-link {
            color: white !important;
        }

        .navbar-brand {
            font-size: 1.6rem;
            font-weight: bold;
        }

        h2 {
            font-weight: bold;
            margin-bottom: 30px;
        }

        .container {
            padding-top: 30px;
            padding-bottom: 50px;
        }

        .btn {
            margin-top: 20px;
        }

        .text-center h2 {
            color: #333;
        }
    </style>
</head>
<body>
    <nav class="navbar navbar-expand-sm navbar-dark">
        <div class="container-fluid">
            <a class="navbar-brand" href="/">User App</a>
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarNav">
                <ul class="navbar-nav me-auto">
                    <li class="nav-item">
                        <a class="nav-link" href="/">Home</a>
                    </li>
                </ul>
            </div>
        </div>
    </nav>

    <div class="container">
        <div class="text-center">
            <h2>Ban User Detail</h2>
        </div>

        <% if (ban) { %> <!-- Check if ban data exists -->
            <div class="bg-white p-4 rounded shadow-sm">
                <p><b>User Name:</b><br> <%= banuser.username %></p>
                <p><b>Ban Reason:</b><br> <%= banuser.ban_reason %></p>
                <p><b>Ban Start:</b><br> <%= new Date(banuser.ban_start).toLocaleDateString('en-GB', { day: '2-digit', month: 'long', year: 'numeric' }) %> </p>
                <p><b>Ban End:</b><br> <%= new Date(banuser.ban_end).toLocaleDateString('en-GB', { day: '2-digit', month: 'long', year: 'numeric' }) %> </p>
                <p><b>Ban User Email:</b><br> <%= banuser.email %></p>
            </div>
        <% } else { %>
            <p>No Ban User found.</p>
        <% } %>

        <div class="text-center">
            <a href="/" class="btn btn-secondary">Back</a>
        </div>
    </div>

</body>
</html>
