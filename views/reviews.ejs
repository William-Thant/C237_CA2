<%/*<h2>Shop Reviews</h2>

<% if (messages.length > 0) { %>
  <ul>
    <% messages.forEach(msg => { %>
      <li><%= msg %></li>
    <% }) %>
  </ul>
<% } %>

<form action="/reviews" method="POST">
  <label for="reviewMsg">Your review:</label><br>
  <textarea name="reviewMsg" rows="4" cols="50" required></textarea><br>

  <label for="rating">Rating (1–5):</label><br>
  <input type="number" name="rating" min="1" max="5" required><br>

  <p><em>All reviews are anonymous. Please remain respectful. No deleting of reviews, but you may post another.</em></p>

  <button type="submit">Submit</button>
</form>

<hr>
<h3>All Reviews</h3>
<% if (reviews.length === 0) { %>
  <p>No reviews yet.</p>
<% } else { %>
  <ul>
    <% reviews.forEach(r => { %>
      <li>
        <strong>Rating:</strong> <%= r.rating %>/5<br>
        <%= r.reviewMsg %>
        <% if (user && user.role === 'admin') { %>
          <form action="/reviews/delete/<%= r.reviewid %>" method="POST" style="display:inline;">
            <button type="submit" onclick="return confirm('Are you sure you want to delete this review?')">Delete</button>
          </form>
        <% } %>
      </li>
      <hr>
    <% }); %>
  </ul>
<% } %>

<p>
  <a href="/reviews">
    <button type="button">Go to Reviews</button>
  </a>
</p>
